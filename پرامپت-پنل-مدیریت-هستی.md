# پرامپت جامع طراحی و توسعه پنل مدیریت فروشگاه «هستی»

> این سند یک پرامپت مستقل و کامل است. می‌توانید آن را عیناً به یک توسعه‌دهنده یا یک ابزار/هوش مصنوعی کدنویسی بدهید تا پنل مدیریت را بسازد.

---

## بخش ۰ | معرفی پروژه (Context)

**هستی** یک فروشگاه اینترنتی ایرانی در حوزه پوشاک و ملزومات حجاب زنانه است (چادر، عبا، شال و روسری، کیف جواهردوزی، شومیز و پالتو). فروشگاه هم به‌صورت آنلاین و هم به‌صورت حضوری (مشهد، بلوار جانباز) فعالیت می‌کند. سایت فروشگاهی (storefront) با HTML/CSS/JavaScript خالص (بدون فریم‌ورک) پیاده‌سازی شده و شامل صفحه اصلی، صفحه دسته‌بندی چادر با فیلتر و مرتب‌سازی، صفحه محصول (PDP) با گالری، نظرات و محصولات مرتبط، و صفحه توضیح شرایط خرید قسطی/دو‌مرحله‌ای است.

ویژگی خاص و متمایز این فروشگاه که باید در پنل مدیریت لحاظ شود:

- **پرداخت دو مرحله‌ای**: ۵۰٪ مبلغ در زمان ثبت سفارش، ۵۰٪ باقی‌مانده حداکثر تا ۱۵ روز پس از تحویل.
- **خرید قسطی**: پیش‌پرداخت حداقل ۳۰٪، پرداخت باقی‌مانده در ۲ تا ۴ قسط ماهانه، هماهنگی زمان‌بندی با پشتیبانی (عمدتاً از طریق واتساپ).
- این دو روش فقط برای سفارش‌های بالای **۳٬۰۰۰٬۰۰۰ تومان** فعال است.
- **ارسال رایگان** برای سفارش‌های بالای **۱۵٬۰۰۰٬۰۰۰ تومان**.
- امکان **خرید حضوری** در فروشگاه مشهد (علاوه بر آنلاین).

اکنون باید یک **پنل مدیریت (Admin Panel)** برای مدیر فروشگاه طراحی و پیاده‌سازی شود که «مغز سایت» باشد: مدیریت کامل محصولات، سفارش‌ها، مشتریان، انبار، تخفیف‌ها، محتوا، نظرات، پیام‌ها و گزارش‌ها.

---

## بخش ۱ | پشته فناوری و اصول کلی (الزامی)

- **فقط HTML5 + CSS3 + JavaScript خالص (Vanilla ES6+)**. استفاده از React/Vue/Angular/Svelte یا هر فریم‌ورک دیگر و همچنین ابزارهای Build (Webpack/Vite و...) **ممنوع** است.
- کدنویسی باید با همان سبک storefront فعلی هم‌خوانی داشته باشد: توابع در قالب IIFE با `"use strict"`، event listener مبتنی بر `addEventListener`، بدون هیچ وابستگی به jQuery.
- استفاده از کتابخانه سبک نموداری از طریق CDN (مثل Chart.js) **مجاز و پیشنهادی** است، چون همچنان JavaScript خالص محسوب می‌شود؛ اگر ترجیح داده نشود، نمودارها باید با SVG خام پیاده‌سازی شوند.
- فرم‌ها و تعامل‌ها باید بدون رفرش کامل صفحه (AJAX/fetch مانند) کار کنند، اما چون بک‌اند واقعی در این فاز جزو دامنه‌ی این پرامپت نیست، باید یک **لایه‌ی داده (data layer)** انتزاعی نوشته شود (به بخش ۹ مراجعه شود) تا بعداً به‌سادگی به API واقعی وصل شود.
- خروجی باید **کاملاً واکنش‌گرا (Responsive)** باشد؛ اولویت با دسکتاپ (چون کاربر اصلی، مدیر فروشگاه، عمدتاً از دسکتاپ استفاده می‌کند) ولی باید در تبلت و موبایل هم کاملاً قابل استفاده باشد (سایدبار جمع‌شونده، جداول قابل اسکرول افقی یا تبدیل به کارت در موبایل).

---

## بخش ۲ | استانداردهای زبان، جهت و قالب‌بندی محتوا (الزامی و حیاتی)

این‌ها مستقیماً از کد storefront استخراج شده‌اند و باید عیناً در پنل مدیریت رعایت شوند تا هماهنگی کامل با سایت اصلی حفظ شود:

| مورد | الزام |
|---|---|
| جهت و زبان | `dir="rtl"` و `lang="fa"` در تمام صفحات |
| فونت | فونت **Sahel** (همان `@font-face` موجود در `test.css`، لود‌شده از jsDelivr) برای تمام متن‌ها؛ فاصله بین حروف و اعداد باید خوانا باشد |
| ارقام | همه‌ی اعداد نمایشی (قیمت، تعداد، درصد، شماره سفارش و...) باید با تابعی مشابه `toPersianNumber()` موجود در کد فعلی به **ارقام فارسی (۰۱۲۳۴۵۶۷۸۹)** تبدیل شوند |
| واحد پول | **تومان** (نه ریال)؛ قیمت‌ها با جداکننده هزارگان مشابه تابع `formatPrice()` فعلی نمایش داده شوند (مثال: `۱۰,۰۰۰,۰۰۰ تومان`) |
| تاریخ | **تقویم شمسی (جلالی)** در همه‌جا (سفارش‌ها، سررسید اقساط، تاریخ عضویت مشتری، تاریخ انتشار مقاله). این الزام از کد واقعی محصول استخراج شده: `new Intl.DateTimeFormat('fa-IR').format(new Date())` در بخش نظرات محصول استفاده شده است. از همین API یا معادل آن استفاده شود |
| اعداد لاتین کنار متن فارسی | مواردی مثل کد سفارش/بارکد/شماره تلفن که معمولاً LTR نمایش داده می‌شوند (`direction: ltr` مثل کلاس `.pdp-code`) باید همین رفتار را حفظ کنند |

**توصیه:** توابع کمکی `toPersianNumber()`, `toEnglishNumber()`, `formatPrice()` که در `test.html`/`chador.html`/`product.html` تکرار شده‌اند، باید در پنل مدیریت در یک فایل مشترک (`admin-shared.js` یا `utils.js`) یک‌بار نوشته و در همه‌ی صفحات import/استفاده شوند تا کد تکراری نشود.

---

## بخش ۳ | هویت بصری و سیستم طراحی پنل (Design System)

پنل باید هم حرفه‌ای و «داشبوردی» به‌نظر برسد (مطابق رفرنس‌های ارائه‌شده) و هم هویت بصری برند هستی را حفظ کند — **رنگ‌های عمومی بنفش/سبز رفرنس‌ها را کپی نکنید**؛ به‌جای آن از پالت رنگی خود برند استفاده شود که در `test.css` تعریف شده:

```
--c-black:  #0a0908   (پس‌زمینه تیره اصلی)
--c-jet:    #22333b   (سرمه‌ای تیره — هدر/فوتر/سایدبار)
--c-linen:  #eae0d5   (کرم روشن — متن روی پس‌زمینه تیره)
--c-tan:    #c6ac8f   (طلایی/بژ — رنگ اصلی برند، دکمه‌ها و حالت فعال)
--c-brown:  #5e503f   (قهوه‌ای — متن ثانویه)
```

رنگ‌های معنایی (Semantic) که باید اضافه شوند — دو مورد اول را از کد فعلی سایت عیناً بردارید تا هماهنگ بماند:

- **موفقیت/سبز**: `#2f7a4f` (دقیقاً همان رنگی که در پیام موفقیت فرم اطلاع‌رسانی موجودی در `product.html` استفاده شده)
- **خطا/قرمز**: `#a8503f` (دقیقاً همان رنگی که برای بج تخفیف، پیام خطای انتخاب سایز و دکمه فعال علاقه‌مندی استفاده شده)
- **هشدار/زرد-نارنجی**: یک رنگ کهربایی هماهنگ با پالت گرم برند (مثلاً `#c9902e`) — برای هشدار اتمام موجودی، سررسید نزدیک قسط
- **اطلاع‌رسانی/آبی خنثی**: یک آبی خاکستری ملایم (مثلاً `#5b7c99`) فقط برای نشان‌های اطلاعاتی خنثی

### چیدمان کلی

- **سایدبار سمت راست** (چون RTL است) با پس‌زمینه تیره `--c-jet`، آیتم‌های منو با آیکون + برچسب، حالت فعال با نوار/پس‌زمینه `--c-tan`. قابلیت جمع‌شدن به حالت فقط-آیکون در صفحات کوچک‌تر (مشابه الگوی سایدبار در رفرنس دوم، اما تیره‌شده متناسب با برند).
- **نوار بالایی (Topbar)**: جعبه جست‌وجوی سراسری (جست‌وجو در محصولات/سفارش‌ها/مشتریان)، زنگ اعلان با نشان تعداد (سفارش جدید، هشدار موجودی، نظر در انتظار تایید، تیکت جدید)، آواتار و نام مدیر با منوی کشویی (پروفایل/خروج).
- **داشبورد اصلی (Home)**: کارت‌های KPI (شبیه کارت‌های «Total Balance/Income/Spending» در رفرنس اول)، نمودار ستونی فروش هفتگی/ماهانه، نمودار حلقه‌ای (Donut) برای پیشرفت هدف ماهانه، فهرست «پرفروش‌ترین محصولات» با تصویر بندانگشتی، تفکیک درآمد بر اساس دسته‌بندی.
- **سطح صفحات فهرست (List views)**: جدول با ستون‌های قابل مرتب‌سازی، فیلتر و جست‌وجو در بالای جدول (مشابه فیلتر خودروها در رفرنس دوم)، صفحه‌بندی (Pagination)، دکمه «افزودن جدید» گوشه بالا.
- **حالت خالی (Empty State)**: از همان الگوی بصری `.shop-empty` در `chador-shop.css` (آیکون + عنوان + توضیح + دکمه اقدام) برای همه‌ی جداول خالی استفاده شود.
- **اعلان‌های موفقیت/خطا**: از همان الگوی Toast شناور پایین صفحه (`.pdp-toast`) استفاده شود تا با storefront یکسان باشد.
- **کارت‌ها**: گوشه‌های گرد ۱۲ تا ۱۶ پیکسل، سایه‌ی نرم (مشابه `box-shadow: 0 20px 40px -24px rgba(94,80,63,.16)` که در `chador-shop.css` تعریف شده)، پس‌زمینه سفید مایل به کرم (`#fffdfa`).

---

## بخش ۴ | نقشه اطلاعاتی پنل (Sitemap)

```
/admin/login.html                → ورود مدیر
/admin/index.html                → داشبورد اصلی
/admin/products.html             → فهرست محصولات
/admin/product-form.html         → افزودن / ویرایش محصول
/admin/categories.html           → دسته‌بندی‌ها و کالکشن‌ها
/admin/orders.html               → فهرست سفارش‌ها
/admin/order-detail.html         → جزئیات سفارش + مدیریت پرداخت اقساطی
/admin/customers.html            → فهرست مشتریان
/admin/customer-detail.html      → پروفایل مشتری + باشگاه مشتریان
/admin/inventory.html            → مدیریت انبار (ورود/خروج/هشدار/گزارش)
/admin/discounts.html            → کدهای تخفیف و کمپین‌ها
/admin/blog.html                 → فهرست مقالات بلاگ
/admin/blog-form.html            → افزودن / ویرایش مقاله
/admin/reviews.html              → مدیریت نظرات محصولات
/admin/messages.html             → پیام‌ها، تیکت پشتیبانی، درخواست اطلاع از موجودی، خبرنامه
/admin/reports.html              → گزارش‌ها و آمار
/admin/settings.html             → تنظیمات فروشگاه
/admin/users.html                → مدیریت کاربران ادمین و دسترسی‌ها
```

**نکته معماری مهم:** بر خلاف الگوی فعلی storefront (که هدر/فوتر در هر صفحه به‌صورت کامل کپی شده)، به‌دلیل تعداد زیاد صفحات پنل (۱۷+ صفحه)، سایدبار و نوار بالا باید در یک تابع مشترک جاوااسکریپت (مثلاً `renderAdminShell()` در `admin-shared.js`) نوشته شده و در تمام صفحات فراخوانی شود تا از تکرار کد و مشکلات نگهداری جلوگیری شود.

---

## بخش ۵ | صفحه ورود (Login)

- فرم شماره موبایل/ایمیل + رمز عبور، یا موبایل + کد یک‌بارمصرف (OTP) — یکی به‌عنوان MVP کافی است (پیشنهاد: موبایل + رمز عبور برای سادگی).
- گزینه «مرا به خاطر بسپار».
- پیام خطای واضح برای ورود ناموفق.
- طراحی بصری هماهنگ با برند: پس‌زمینه تیره (`--c-jet`/`--c-black`) با تصویر/لوگوی هستی، فرم در کارت روشن وسط صفحه.
- **هشدار امنیتی که باید در کد به‌صورت کامنت درج شود:** پیاده‌سازی صرفاً HTML/CSS/JS بدون بک‌اند نمی‌تواند احراز هویت واقعی و امن ارائه دهد. این صفحه باید به‌عنوان **رابط کاربری آماده اتصال به سرویس احراز هویت واقعی** ساخته شود (فرم و state مدیریت شود، اما اعتبارسنجی نهایی باید بعداً از سمت سرور انجام گیرد).

---

## بخش ۶ | داشبورد اصلی (Home Dashboard)

### کارت‌های KPI (ردیف بالا)
- فروش امروز / این هفته / این ماه (تومان)
- تعداد سفارش‌های جدید (در انتظار تایید)
- تعداد کل مشتریان و رشد نسبت به ماه قبل
- مبلغ اقساط/مرحله‌دوم **معوق و سررسید نزدیک** (ویجت اختصاصی مهم به‌دلیل مدل کسب‌وکار هستی)

### ویجت‌های میانی
- نمودار روند فروش (خطی یا ستونی) با سوییچ روزانه/هفتگی/ماهانه
- نمودار حلقه‌ای «پیشرفت هدف فروش ماهانه»
- فهرست «پرفروش‌ترین محصولات» (تصویر کوچک + نام + تعداد فروش + درآمد)
- تفکیک درآمد بر اساس **دسته‌بندی** (چادر / عبا / شال و روسری / کیف جواهردوزی / شومیز و پالتو) به‌صورت نمودار حلقه‌ای چندبخشی

### ویجت‌های هشدار/اقدام سریع
- محصولات رو به اتمام (کمتر از آستانه موجودی)
- سفارش‌های در انتظار تایید پرداخت
- نظرات در انتظار تایید
- تیکت‌های پشتیبانی باز
- درخواست‌های «اطلاع از موجود شدن» برای محصولاتی که به‌تازگی موجود شده‌اند (اقدام: ارسال گروهی اطلاع‌رسانی)

---

## بخش ۷ | ماژول‌های مدیریتی (شرح کامل فیلد‌به‌فیلد)

### ۷.۱ مدیریت محصولات

> **توجه:** فیلد «برند» عمداً حذف شده است چون هستی یک برند تک‌مالک است (در هیچ‌کجای storefront مفهوم چندبرندی وجود ندارد). به‌جای آن، «دسته‌بندی» و «کالکشن» دو محور اصلی سازمان‌دهی محصول هستند (دقیقاً مطابق مگامنوی سایت که این دو را جدا از هم نمایش می‌دهد).

**صفحه فهرست محصولات:**
- نمایش جدولی یا کارتی (Toggle)
- ستون‌ها: تصویر شاخص، نام، دسته‌بندی، کالکشن‌ها، قیمت (قبل/بعد تخفیف)، موجودی کل، وضعیت (فعال/پیش‌نویس/ناموجود)، بازدید، تاریخ ثبت
- فیلتر: دسته‌بندی، کالکشن، جنس پارچه، وضعیت موجودی، بازه قیمت، جست‌وجوی متنی (نام/کد کالا)
- عملیات گروهی: تغییر وضعیت، اعمال تخفیف روی چند محصول، درون‌ریزی/برون‌بری CSV، حذف گروهی

**فرم افزودن/ویرایش محصول:**

| فیلد | توضیح |
|---|---|
| نام محصول | مثال: «چادر فاخر نگین باران» |
| اسلاگ/شناسه یکتا (id) | برای URL محصول (`product.html?id=...`)، خودکار از نام یا دستی |
| کد کالا (SKU) | مطابق فیلد `code` موجود در دیتای فعلی (مثال: `HJ-CHD-001`) |
| بارکد | برای اسکن در فروشگاه حضوری، امکان تولید و چاپ بارکد |
| دسته‌بندی | یکی از: چادر مجلسی / عبا / شال و روسری / کیف جواهردوزی / شومیز / پالتو (تاکسونومی قابل مدیریت، بخش ۷.۲) |
| کالکشن (چندانتخابی) | کالکشن اربعین، کالکشن زمستانی، ست‌های پیشنهادی هستی، کالکشن اقتصادی، ... (بخش ۷.۲) |
| جنس پارچه | تاکسونومی قابل مدیریت (نمونه‌های موجود: کرپ حریر، کرپ کریستال، نسیم مشکی، ژرسه سنگین، ساتن مشکی، کرپ اسپاندکس) — این فیلد باید هم در فرم محصول و هم به‌صورت فیلتر در storefront قابل استفاده باشد |
| سایز(ها) | چندمقداری؛ برای عبا/شومیز: `S,M,L,XL`؛ برای چادر: `یک‌سایز` |
| قد (سانتی‌متر) | چندمقداری عددی؛ برای چادر: ۱۵۵ تا ۱۷۰؛ برای عبا: ۱۳۰ تا ۱۴۵ (بازه قابل تنظیم بر اساس نوع محصول) |
| رنگ | نام رنگ + نمونه‌رنگ (swatch)؛ هر رنگ می‌تواند مجموعه تصویر جداگانه داشته باشد |
| موجودی به‌تفکیک تنوع | ترکیب سایز × قد × رنگ → هرکدام عدد موجودی و بارکد مجزا (نه فقط یک فلگ موجود/ناموجود ساده) |
| قیمت قبل از تخفیف | تومان |
| درصد تخفیف | عدد ۰ تا ۱۰۰؛ قیمت نهایی باید **خودکار محاسبه و نمایش داده شود** (دقیقاً همان فرمول موجود در کد فعلی: `oldPrice - oldPrice × discount / 100`) |
| بهای تمام‌شده (Cost Price) | **فیلد جدید، پیشنهادی** — برای امکان محاسبه سود در گزارش‌ها؛ فقط داخلی و غیرقابل‌مشاهده برای مشتری |
| تصاویر محصول | چندتایی، قابل کشیدن‌ورها (drag reorder)، تصویر اول = اصلی، تصویر دوم = نمایش هاور (مطابق افکت هاور موجود در کارت محصول storefront) |
| توضیحات محصول | متن بلند |
| ویژگی‌های محصول | فهرست نکته‌ای (bullet list) — مطابق بخش «ویژگی‌های محصول» در PDP |
| نحوه شست‌وشو و نگهداری | فهرست نکته‌ای — مطابق بخش مشابه در PDP |
| وضعیت | فعال / پیش‌نویس / ناموجود / متوقف‌شده |
| نمایش در صفحه اصلی | فلگ Boolean — چون در حال حاضر گرید محصولات صفحه اصلی و کارت‌های دسته‌بندی به‌صورت هاردکد در HTML هستند، پنل باید کنترل کامل روی این‌که چه محصولاتی در صفحه اصلی نمایش داده شوند را بدهد |
| سئو | عنوان متا، توضیح متا (مطابق تگ `<meta name="description">` موجود در هر صفحه) |
| محصولات مرتبط | خودکار بر اساس دسته‌بندی (مطابق منطق فعلی PDP) یا امکان بازنویسی دستی |

**اقدامات دیگر:** کپی/تکثیر محصول (برای ساخت سریع نسخه‌های رنگ/تنوع جدید).

---

### ۷.۲ مدیریت دسته‌بندی و کالکشن (بخش جدید، جایگزین «دسته‌بندی» ساده در پیش‌نویس اولیه)

Storefront دو سیستم دسته‌بندی **مجزا** دارد که باید هر دو در پنل مدیریت‌شدنی باشند:

**دسته‌بندی محصول (Category)** — ستون اول مگامنو:
- CRUD کامل (افزودن/ویرایش/حذف/ترتیب نمایش)
- فیلدها: نام، اسلاگ، تصویر (برای کارت‌های دایره‌ای صفحه اصلی)، سئو، ترتیب در منو

**کالکشن (Collection)** — ستون دوم مگامنو، برای کمپین‌های فصلی/مناسبتی:
- CRUD کامل
- فیلدها: نام، تصویر بنر، بازه تاریخ فعال‌بودن (برای کالکشن‌های فصلی مثل «کالکشن زمستانی» یا مناسبتی مثل «کالکشن اربعین»)، فلگ «نمایش در صفحه اصلی»، سئو

---

### ۷.۳ مدیریت سفارش‌ها (بازنویسی کامل بخش پرداخت)

**فهرست سفارش‌ها — ستون‌ها:**
شماره سفارش، نام مشتری، تاریخ (شمسی)، تعداد اقلام، مبلغ کل، **نوع پرداخت** (کامل / دو‌مرحله‌ای / قسطی)، وضعیت پرداخت، وضعیت سفارش، نوع تحویل (پستی / حضوری)، کد رهگیری

**فیلتر:** بازه تاریخ، وضعیت سفارش، وضعیت پرداخت، نوع پرداخت، نوع تحویل

**جزئیات سفارش:**

1. **اطلاعات مشتری**: نام، تلفن (لینک مستقیم به واتساپ، چون پشتیبانی هستی عمدتاً از طریق واتساپ هماهنگ می‌شود)، آدرس (در صورت ارسال پستی) یا فلگ «تحویل حضوری در فروشگاه».
2. **اقلام سفارش**: نام محصول، تصویر، سایز/قد/رنگ انتخابی، تعداد، قیمت واحد، جمع.
3. **خلاصه مالی**: جمع اقلام، هزینه ارسال (رایگان اگر بالای ۱۵ میلیون تومان)، تخفیف اعمال‌شده (کد تخفیف در صورت استفاده)، مبلغ نهایی.
4. **بخش پرداخت (بسته به نوع سفارش):**

   **الف) پرداخت کامل:** وضعیت پرداخت (پرداخت‌شده/در انتظار)، تاریخ پرداخت، روش پرداخت.

   **ب) پرداخت دو‌مرحله‌ای:**
   | مرحله | مبلغ | وضعیت | تاریخ سررسید | اقدام |
   |---|---|---|---|---|
   | مرحله اول (۵۰٪) | خودکار محاسبه | پرداخت‌شده / در انتظار | زمان ثبت سفارش | — |
   | مرحله دوم (۵۰٪ باقی‌مانده) | خودکار محاسبه | پرداخت‌شده / در انتظار / معوق | حداکثر ۱۵ روز پس از تاریخ تحویل | دکمه «ارسال لینک پرداخت» یا «ارسال یادآوری واتساپ» |

   **ج) خرید قسطی:** جدول کامل اقساط با ستون‌های: شماره قسط، مبلغ، تاریخ سررسید (شمسی)، وضعیت (پرداخت‌شده/در انتظار/معوق)، تاریخ پرداخت واقعی. امکان ویرایش دستی زمان‌بندی اقساط (چون طبق شرایط فعلی هماهنگی با پشتیبانی انجام می‌شود، نه گیت‌وی خودکار). دکمه «ارسال یادآوری» برای هر قسط (پیامک/واتساپ).
   
   > سیستم باید هنگام ثبت سفارش قسطی/دومرحله‌ای، **اعتبارسنجی حداقل مبلغ سفارش (۳٬۰۰۰٬۰۰۰ تومان)** را انجام دهد و در غیر این‌صورت این گزینه‌ها را غیرفعال کند.

5. **وضعیت سفارش (Workflow):** در انتظار تایید → در حال آماده‌سازی → ارسال شده / آماده تحویل حضوری → تحویل داده شده → لغو شده / مرجوعی. هر تغییر وضعیت باید در تاریخچه سفارش ثبت شود.
6. **ارسال:** کد رهگیری، انتخاب شرکت پستی/باربری، چاپ برچسب پستی.
7. **چاپ فاکتور**: خروجی قابل چاپ/PDF شامل جزئیات کامل سفارش و مشتری.
8. **دکمه اقدام سریع**: «ارسال پیام به مشتری از طریق واتساپ» (با پیش‌متن آماده، چون واتساپ کانال اصلی ارتباطی فروشگاه با شماره ۰۹۱۵۲۵۰۰۵۵۳ است).
9. **یادداشت داخلی سفارش** (فقط برای تیم، مشتری نمی‌بیند).

---

### ۷.۴ مدیریت مشتریان

**فهرست مشتریان:** نام، تلفن، شهر، تعداد سفارش، مجموع مبلغ خرید، امتیاز باشگاه مشتریان، سطح عضویت، تاریخ عضویت (شمسی).

**پروفایل مشتری:**
- اطلاعات تماس و آدرس‌های ذخیره‌شده
- تاریخچه کامل سفارش‌ها با لینک به جزئیات هر سفارش
- لیست علاقه‌مندی‌های مشتری (Wishlist) — از قابلیت موجود در storefront (`hasti_wishlist`)
- نظراتی که ثبت کرده
- درخواست‌های «اطلاع از موجود شدن» که ثبت کرده
- برچسب/تگ داخلی (مثلاً «مشتری ویژه»، «معوق پرداخت»)
- یادداشت‌های داخلی مدیر درباره مشتری

**باشگاه مشتریان:**
- سیستم سطح‌بندی بر اساس مجموع خرید (مثلاً برنزی/نقره‌ای/طلایی)
- قوانین کسب امتیاز (مثلاً هر ۱۰۰ هزار تومان خرید = X امتیاز)
- امکان تبدیل امتیاز به تخفیف
- تنظیم/اصلاح دستی امتیاز توسط مدیر

**مشترکین خبرنامه (بخش جدید):** فرم خبرنامه در فوتر تمام صفحات storefront وجود دارد؛ پنل باید فهرست ایمیل‌های ثبت‌شده، تاریخ ثبت‌نام و امکان برون‌بری (Export) برای کمپین‌های ایمیلی را داشته باشد.

**برون‌بری (Export)** فهرست مشتریان/مشترکین به CSV برای بازاریابی.

---

### ۷.۵ مدیریت انبار

- **موجودی به‌تفکیک تنوع محصول** (سایز × قد × رنگ)، نه فقط یک عدد کلی برای هر محصول.
- **ثبت ورودی**: افزودن موجودی از تامین‌کننده/تولید داخلی، با تاریخ و شماره فاکتور خرید.
- **ثبت خروجی**: کسر خودکار از فروش، و امکان ثبت دستی خروجی (آسیب‌دیدگی، نمونه فروشگاهی، مرجوعی).
- **هشدار اتمام موجودی**: آستانه هشدار قابل‌تنظیم برای هر محصول/تنوع؛ نمایش در داشبورد و ارسال اعلان.
- **گزارش موجودی**: ارزش‌گذاری انبار (بر اساس بهای تمام‌شده)، کالاهای کم‌فروش/راکد، تاریخچه تغییرات موجودی هر محصول.
- در صورت وجود انبار جداگانه برای فروشگاه حضوری و انبار آنلاین، امکان تفکیک موجودی بر اساس محل (این مورد باید با کارفرما تایید شود — به بخش ۱۰ مراجعه کنید).

---

### ۷.۶ مدیریت تخفیف و کمپین

**کد تخفیف:**
| فیلد | توضیح |
|---|---|
| کد | مثال: `HASTI20` |
| نوع | تخفیف درصدی یا تخفیف مبلغ ثابت (تومان) |
| مقدار | عدد درصد یا مبلغ |
| حداقل مبلغ سفارش | برای فعال‌شدن کد |
| سقف استفاده کل | تعداد دفعات مجاز استفاده |
| سقف استفاده هر مشتری | معمولاً یک‌بار |
| قابل‌اعمال روی | همه محصولات / دسته‌بندی خاص / کالکشن خاص / محصولات مشخص |
| بازه اعتبار | تاریخ شروع و پایان (شمسی) |
| وضعیت | فعال/غیرفعال |

**کمپین مناسبتی:** امکان اعمال گروهی تخفیف روی یک کالکشن کامل (مثال: «۲۰٪ تخفیف کالکشن اربعین») که در بج تخفیف کارت محصول به‌صورت خودکار نمایش داده می‌شود (`product-card__badge` موجود در کد فعلی). زمان‌بندی شروع/پایان خودکار کمپین.

**گزارش استفاده از کد تخفیف:** تعداد استفاده، مبلغ تخفیف داده‌شده، سفارش‌های مرتبط.

---

### ۷.۷ مدیریت بلاگ

> توجه: در حال حاضر storefront صفحه بلاگ ندارد. این ماژول باید طوری ساخته شود که هم‌زمان زمینه‌ساز افزودن صفحه بلاگ به سایت اصلی (با همان زبان طراحی: فونت Sahel، پالت تیره/کرم) نیز باشد.

- افزودن/ویرایش مقاله: عنوان، اسلاگ، تصویر شاخص، دسته‌بندی مقاله (تاکسونومی مجزا از دسته‌بندی محصول)، محتوا (ویرایشگر متن غنی — حداقل Bold/Italic/لیست/لینک/تصویر)، برچسب‌ها، نویسنده، تاریخ انتشار (شمسی)، وضعیت (پیش‌نویس/منتشرشده/زمان‌بندی‌شده).
- دسته‌بندی مقالات: CRUD مجزا.
- سئو: عنوان متا، توضیح متا، تصویر شبکه‌اجتماعی (OG Image)، آدرس Canonical.

---

### ۷.۸ مدیریت نظرات

Storefront همین حالا سیستم نظردهی با امتیاز ستاره‌ای (۱ تا ۵)، نام و متن نظر دارد (`pdpReviewForm`/`pdpReviewList`). پنل باید:

- فهرست همه نظرات همه محصولات با فیلتر بر اساس وضعیت (در انتظار تایید / تاییدشده / رد‌شده) و محصول.
- **تایید / رد** نظر پیش از نمایش عمومی.
- **پاسخ فروشگاه** به نظر (باید به‌عنوان قابلیت جدید در PDP نیز اضافه شود تا پاسخ زیر نظر نمایش داده شود).
- گزارش نظر نامناسب/اسپم.
- محاسبه و نمایش خودکار میانگین امتیاز هر محصول (برای جایگزینی امتیاز ثابت فعلی ۴.۹ در صفحه اصلی با عدد واقعی محاسبه‌شده).

---

### ۷.۹ پیام‌ها، تیکت پشتیبانی و درخواست‌های اطلاع‌رسانی

- **تیکت پشتیبانی**: عنوان، مشتری، اولویت، وضعیت (باز/در حال بررسی/بسته)، تخصیص به کارشناس، تاریخچه گفت‌وگو.
- **پیام‌های تماس با ما**: (نیازمند افزودن یک فرم تماس در storefront، چون فعلاً فقط لینک واتساپ/اینستاگرام وجود دارد — این را به‌عنوان پیش‌نیاز پیشنهادی به کارفرما اعلام کنید).
- **درخواست اطلاع از موجود شدن (بخش جدید، حیاتی)**: storefront دارای فرمی است (`pdpNotifyForm`) که ایمیل/موبایل مشتریان علاقه‌مند به محصولات ناموجود را ذخیره می‌کند. پنل باید:
  - فهرست این درخواست‌ها به‌تفکیک محصول
  - وقتی مدیر موجودی محصول را فعال می‌کند، امکان **ارسال گروهی پیامک/ایمیل اطلاع‌رسانی** به همه درخواست‌دهندگان آن محصول با یک کلیک

---

### ۷.۱۰ گزارش‌ها و آمار

- فروش روزانه / ماهانه / سالانه (نمودار + جدول، با مقایسه به دوره قبل)
- پرفروش‌ترین محصولات و پرفروش‌ترین دسته‌بندی‌ها
- **سود** (بر اساس فیلد جدید «بهای تمام‌شده» در محصول)
- مشتریان برتر بر اساس مبلغ خرید
- نرخ بازگشت مشتری (نسبت مشتریانی که بیش از یک سفارش ثبت کرده‌اند)
- نرخ تبدیل (نیازمند اتصال به سرویس آنالیتیکس مثل Google Analytics؛ در این فاز فقط UI محل نمایش آماده شود و به‌عنوان وابستگی خارجی علامت‌گذاری شود)
- **سبد خرید رها شده**: توجه شود که در حال حاضر سبد خرید storefront فقط یک شمارنده (`hasti_cart_count`) در localStorage است و اقلام واقعی سبد ذخیره نمی‌شود؛ برای این گزارش باید یک مدل داده واقعی «سبد خرید» (شامل اقلام و شناسه مرورگر/کاربر) طراحی شود — این را به‌عنوان نیازمندی فنی جدید علامت‌گذاری کنید.
- **گزارش سلامت پرداخت‌های اقساطی/دومرحله‌ای** (اختصاصی مدل کسب‌وکار هستی): مجموع مطالبات معوق، اقساط سررسیدشده در ۷ روز آینده.
- خروجی همه گزارش‌ها به Excel/PDF.

---

### ۷.۱۱ تنظیمات فروشگاه (بخش جدید)

- **اطلاعات کلی**: نام فروشگاه، لوگو، شماره تماس/واتساپ (پیش‌فرض: ۰۹۱۵۲۵۰۰۵۵۳)، آدرس فروشگاه حضوری (پیش‌فرض: مشهد، بلوار جانباز، مجتمع اداری پاژ شماره ۲، طبقه سوم، واحد ۳۰۸)، ساعات کاری (شنبه تا پنج‌شنبه ۱۰:۳۰-۱۳:۳۰ و ۱۷-۲۱، جمعه عصر متغیر)، لینک شبکه‌های اجتماعی (اینستاگرام، Threads، واتساپ).
- **تنظیمات ارسال**: آستانه ارسال رایگان (پیش‌فرض ۱۵٬۰۰۰٬۰۰۰ تومان)، هزینه ارسال بر اساس مناطق.
- **تنظیمات پرداخت اقساطی/دومرحله‌ای**: فعال/غیرفعال‌سازی کلی قابلیت، حداقل مبلغ سفارش مجاز (پیش‌فرض ۳٬۰۰۰٬۰۰۰ تومان)، درصد پیش‌پرداخت قسطی (پیش‌فرض ۳۰٪)، بازه تعداد اقساط مجاز (پیش‌فرض ۲ تا ۴)، نسبت تقسیم دومرحله‌ای (پیش‌فرض ۵۰/۵۰)، مهلت مرحله دوم پس از تحویل (پیش‌فرض ۱۵ روز).
- **مدیریت متن نوار اعلانات (Ticker)**: در حال حاضر متن‌های نوار بالای سایت و تیکر زیر هدر به‌صورت هاردکد در HTML هستند؛ باید قابل ویرایش از پنل شوند.
- **مدیریت محتوای صفحه اصلی**: تصاویر و متن اسلایدر هیرو، تصویر و متن بنر «امضای اصالت»، متن بخش «درباره هستی» و بنر معرفی برند.

---

### ۷.۱۲ مدیریت کاربران ادمین و دسترسی‌ها

- افزودن کاربر ادمین با نقش مشخص: مدیر کل، مدیر محصولات، مدیر سفارش‌ها، پشتیبانی/تیکت.
- تعریف دسترسی هر نقش به ماژول‌ها (Read/Write/None).
- گزارش فعالیت (Activity Log): چه کسی چه تغییری در چه زمانی داده است — مهم برای شفافیت در محیط چند‌نفره.

---

## بخش ۸ | پیشنهاد ساختار داده (Data Schema)

این ساختارها باید هم‌خوان با مدل داده‌ی فعلی storefront (آرایه `PRODUCTS` در `product.html` و `CHADOR_PRODUCTS` در `chador.html`) طراحی شوند تا اتصال بعدی به بک‌اند واقعی ساده باشد:

```js
// محصول
{
  id: "chador-negin-baran",
  code: "HJ-CHD-001",
  barcode: "8901234567890",
  name: "چادر فاخر نگین باران",
  category: "chador",              // چادر مجلسی | عبا | شال-و-روسری | کیف-جواهردوزی | شومیز | پالتو
  collections: ["arbaeen", "eco"], // چندانتخابی
  fabric: "کرپ حریر",
  colors: [{ name: "مشکی", hex: "#111111", images: ["..."] }],
  sizes: ["یک‌سایز"],
  lengths: [155, 160, 165, 170],
  variants: [                      // موجودی به‌تفکیک تنوع
    { size: "یک‌سایز", length: 160, color: "مشکی", stock: 12, barcode: "..." }
  ],
  oldPrice: 10000000,
  discount: 26,
  costPrice: 4200000,              // فقط داخلی
  images: ["./04.webp", "./04 (1).webp"],
  description: "...",
  features: ["...", "..."],
  care: ["...", "..."],
  status: "published",             // published | draft | out_of_stock | discontinued
  featuredHome: true,
  seo: { title: "...", description: "..." },
  views: 980,
  createdAt: "1403-04-02",
  updatedAt: "1403-06-01"
}

// سفارش
{
  orderNumber: "HST-10245",
  customerId: "c_1029",
  items: [{ productId: "...", size: "...", length: 160, color: "مشکی", qty: 1, unitPrice: 7400000 }],
  shipping: { type: "post", cost: 0, address: {...} }, // یا type: "pickup"
  paymentPlan: {
    type: "installment",          // full | two_stage | installment
    totalAmount: 7400000,
    downPayment: 2220000,
    installments: [
      { index: 1, amount: 2593333, dueDate: "1403-07-10", status: "paid",   paidAt: "1403-07-09" },
      { index: 2, amount: 2593334, dueDate: "1403-08-10", status: "pending" }
    ]
  },
  orderStatus: "processing",       // pending | processing | shipped | delivered | cancelled | returned
  trackingCode: null,
  source: "online",                // online | in_store
  createdAt: "1403-06-20"
}

// مشتری
{
  id: "c_1029",
  name: "فاطمه ...",
  phone: "0915...",
  city: "مشهد",
  totalOrders: 4,
  totalSpent: 28500000,
  loyaltyPoints: 285,
  loyaltyTier: "silver",
  wishlist: ["chador-negin-baran"],
  tags: ["مشتری ویژه"],
  joinDate: "1402-11-03"
}
```

ساختارهای مشابه باید برای `DiscountCode`، `Review`، `Ticket`، `BlogPost`، `RestockRequest` و `NewsletterSubscriber` نیز تعریف شوند، با همین منطق (فیلدهای واضح، تاریخ شمسی، تومان).

---

## بخش ۹ | الزامات فنی پیاده‌سازی

- **لایه داده انتزاعی**: یک ماژول `dataService.js` بنویسید که توابعی مثل `getProducts()`, `saveProduct()`, `getOrders()` را ارائه دهد. در این فاز، این توابع از یک فایل `mock-data.js` (آرایه‌های JS شبیه نمونه بالا) داده برمی‌گردانند؛ اما باید طوری نوشته شوند (مثلاً به‌صورت `async function` که Promise برمی‌گردانند) که بعداً با تغییر بدنه تابع به `fetch()` واقعی، بقیه کد پنل نیازی به تغییر نداشته باشد.
- **localStorage به‌عنوان لایه موقت**: مشابه الگوی فعلی storefront (`hasti_cart_count`, `hasti_wishlist`, `hasti_reviews_*`)، برای تعاملات نمایشی (مثل ذخیره فیلترهای انتخابی مدیر یا حالت جمع‌شدن سایدبار) استفاده شود، اما داده‌های تجاری حساس (سفارش، پرداخت، موجودی) نباید فقط در localStorage بمانند — باید به‌وضوح TODO گذاشته شود که این‌ها نیازمند بک‌اند/پایگاه‌داده واقعی هستند.
- **جدول‌ها**: مرتب‌سازی، فیلتر، صفحه‌بندی سمت کلاینت (چون داده فعلاً محلی است)؛ ساختار کد طوری باشد که جابه‌جایی به صفحه‌بندی سمت سرور آسان باشد.
- **اعتبارسنجی فرم‌ها**: پیام خطای فارسی و واضح، مشابه الگوی `pdpHint` موجود در PDP («لطفاً ابتدا سایز را انتخاب کنید»).
- **دسترسی‌پذیری (Accessibility)**: `aria-label` مناسب، ناوبری کامل با کیبورد در منوها و فرم‌ها، `:focus-visible` مشخص (مطابق تعریف موجود در `test.css`).
- **کارایی**: `loading="lazy"` برای تصاویر، Debounce روی ورودی‌های جست‌وجو، بارگذاری تدریجی جداول بزرگ.

---

## بخش ۱۰ | نکات و مفروضاتی که باید با کارفرما تایید شود

1. آیا چند مدیر/نقش هم‌زمان نیاز است یا یک کاربر ادمین کافی است؟
2. تکنولوژی بک‌اند/پایگاه‌داده واقعی چه خواهد بود؟ (این پرامپت فقط رابط کاربری HTML/CSS/JS را پوشش می‌دهد)
3. آیا صفحه بلاگ و فرم تماس با ما به سایت اصلی (storefront) نیز اضافه خواهد شد؟
4. آیا موجودی انبار فروشگاه حضوری از انبار آنلاین جداست یا مشترک؟
5. آیا در آینده درگاه پرداخت آنلاین برای مراحل دوم/اقساط اضافه می‌شود، یا روند هماهنگی واتساپی فعلی ادامه می‌یابد؟

---

## بخش ۱۱ | خروجی نهایی مورد انتظار

مجموعه فایل‌های HTML/CSS/JS مطابق ساختار بخش ۴، به همراه:
- `admin/assets/admin-shared.css` (سیستم طراحی مشترک: رنگ‌ها، تایپوگرافی، کامپوننت‌های تکرارشونده مثل کارت/جدول/دکمه/Toast)
- `admin/assets/admin-shared.js` (سایدبار، Topbar، توابع کمکی فارسی‌سازی اعداد و تاریخ، Toast)
- `admin/assets/data/mock-data.js` (داده نمایشی برای همه ماژول‌ها، هم‌ساختار با بخش ۸)
- کدها باید تمیز، ماژولار، و با کامنت‌گذاری کافی (فارسی یا انگلیسی) باشند تا توسعه‌دهنده بعدی به‌راحتی بتواند به بک‌اند واقعی وصل کند.
